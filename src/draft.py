# Detect hand motion

# check if hand available is True
# record handpose at next frame
# compute distance between two poses
# if distance > threshow : append record that pose
# else: restart pose

import cv2
import pickle
import numpy as np
import mediapipe as mp
mp_drawing = mp.solutions.drawing_utils
mp_hands = mp.solutions.hands







